spring:
  datasource:
    url: jdbc:mysql://db:3306/tfg_db_prod
    username: tfg_user
    password: tfg_password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

server:
  port: 8080

springdoc:
  api-docs:
    path: /api-docs
